1. c) Character

2. b) String

3. let and const both used for declaring variable/identifyers but const make sure that once we assign value to variable it will not change its value and let allow us to change the value.

4. a) ...

5) a) [2, 4, 6]

6) c) They don't have there own this

7) c) querySelector('.className')

8) b) 'object'

9) b) trim()

10) Event.preventDefault() remove the default behaviour of the elements. Ex: In form when we click submit button then the page reload automatically but with preventDefault() we can stop page reload.

11) c) forEach()

12) a) [1, 2, 3, 4]

13) Synchronous code runs the code line by line. It mean that when one line complete it's execution then it move to next line. If the current execution line take time in that case next line need to wait until the current line to complete execution.

    Asynchronous code runs the code in different way it says that if a line takes time to complete it's execution then please run that line in background and mean while run the next line. Once the background work done it's computing execute that part of code.

14) d) DOMContentLoaded

15) For simplify the use of object and it's nested data.
    When we are working with object we know that the object will be complex when they are nested and it's bad practice to use dot notation while working with objects. At that time we destructure the object in such a way that we can use direct variable name insead of dot notation.
